# Your wallet private key (never commit the real .env file)
TESTNET_PRIVATE_KEY=0xYourPrivateKeyHere

# Set after deploying — used to fund the paymaster
PAYMASTER_ADDRESS=0x
VOTING_ADDRESS=0x

# Optional: poll title and proposals (override defaults in Deploy.s.sol)
POLL_TITLE=Which ADI feature should ship next?
PROPOSAL_0=Native mobile SDK
PROPOSAL_1=WebAssembly runtime
PROPOSAL_2=Cross-chain bridge UI
