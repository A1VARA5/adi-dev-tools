{
  "name": "counter-dapp",
  "version": "1.0.0",
  "description": "ADI Chain counter dApp — Hardhat example",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "node deploy.mjs",
    "deploy:mainnet": "NETWORK=mainnet node deploy.mjs"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "ethers": "^6.0.0"
  },
  "devDependencies": {
    "hardhat": "^3.1.5",
    "hardhat-adi-network": "^0.1.2",
    "@nomicfoundation/hardhat-ignition": "^3.0.0",
    "@nomicfoundation/hardhat-verify": "^3.0.0"
  }
}
