{
  "name": "hardhat-adi-network",
  "version": "0.1.0",
  "description": "Hardhat plugin that auto-injects ADI testnet and mainnet network configs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "lint": "tsc --noEmit"
  },
  "keywords": ["hardhat", "hardhat-plugin", "adi", "adi-chain", "adi-foundation"],
  "license": "MIT",
  "dependencies": {
    "@adi-foundation/sdk": "workspace:*"
  },
  "devDependencies": {
    "hardhat": "^3.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0"
  },
  "peerDependencies": {
    "hardhat": ">=3.0.0"
  }
}
