{
  "name": "@adi-foundation/sdk",
  "version": "0.1.0",
  "description": "JavaScript/TypeScript SDK for ADI Chain — pre-configured ethers.js + viem helpers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "lint": "tsc --noEmit"
  },
  "keywords": ["adi", "adi-chain", "adi-foundation", "zkrollup", "evm", "blockchain"],
  "license": "MIT",
  "dependencies": {
    "ethers": "^6.11.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0"
  },
  "peerDependencies": {
    "viem": ">=2.0.0"
  },
  "peerDependenciesMeta": {
    "viem": { "optional": true }
  }
}
