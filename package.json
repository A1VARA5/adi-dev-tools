{
  "name": "adi-dev-tools",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/A1VARA5/adi-dev-tools"
  },
  "homepage": "https://github.com/A1VARA5/adi-dev-tools#readme",
  "bugs": {
    "url": "https://github.com/A1VARA5/adi-dev-tools/issues"
  }
}
